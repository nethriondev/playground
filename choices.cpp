do {
    // 1. SHOW MENU
    cout << "1. Add Student" << endl;
    cout << "2. Display Student" << endl;
    cout << "3. Exit" << endl;
    cin >> choice;
    
    // 2. DO ACTION based on choice
    if (choice == 1) {
        // add student code
    }
    else if (choice == 2) {
        // display student code
    }
    
} while (choice != 3);  // 3. REPEAT until user chooses Exit